[variables]
PYTHON_VERSION = "3.11.8"

[phases.setup]
nixPkgs = ["python311", "pip"]

[phases.install]
cmds = [
  "python --version",
  "pip install -r requirements.txt"
]

[start]
cmd = "python bot.py"
